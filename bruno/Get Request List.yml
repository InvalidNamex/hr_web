info:
  name: Get Request List
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/Web/GetRequestList?requestTypeId=1&pageNo=1&pageSize=10"
  headers:
    - name: ServiceKey
      value: "1111111"
  params:
    - name: requestTypeId
      value: "1"
      type: query
    - name: pageNo
      value: "1"
      type: query
    - name: pageSize
      value: "10"
      type: query
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: https://natureccmpany.homeip.net:57571

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
